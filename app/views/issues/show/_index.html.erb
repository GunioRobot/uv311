<div class="issue">
  <div class="tally span-3" >
    <label><%= @issue.votes.count %></label>
    <span>Agree</span>
  </div>
  <div class="span-18">
    <h3><%= @issue.title %></h3>
  <p>
    <pre><%=h @issue.description %></pre>
  </p>
  </div>
</div>

<div id="new_comment">
<h2>Add a comment:</h2>

<% form_for(@comment) do |f| %>  
  <%= f.hidden_field :issue_id %>
  <%= f.label :author, 'Your Name' %>
  <%= f.text_field :author %>
  <%= f.label :body, 'Body' %>
  <%= f.text_area :body, :cols => 30, :rows => 5 %><br/>
  <%= submit_tag 'Add Comment' %>
<% end %>
</div>

<br/>

<div id="comments">
  <% @comments.each do |comment| %>
    <div class="comment">
      <p>
        <strong><%=h comment.author %></strong> said:<br/>
        <%=h comment.body %>
      </p>
    </div>
  <% end %>
</div>
